// BOJ 풀이 스켈레톤 (TypeScript 5+)
// solve 함수를 구현하세요.

/**
 * @param 파라미터 - 문제에 맞게 설계
 * @returns 결과
 */
function solve(/* 파라미터: type */): string {
  // TODO: 알고리즘 구현
  return '';
}

// 입력 처리
const readline = require('readline');
const rl = readline.createInterface({ input: process.stdin });
const lines: string[] = [];

rl.on('line', (line: string) => lines.push(line.trim()));
rl.on('close', () => {
  let idx = 0;
  // TODO: 입력 파싱 후 solve() 호출
  // const n = parseInt(lines[idx++]);
  // console.log(solve(n));
});
